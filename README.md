# 📡 Primerica Operations Hub

A fully client-side call center operations dashboard for CDR analytics, agent adherence tracking, attendance reporting, and staffing visibility. Runs entirely in the browser — no server, no login, no installation required.

**Live Dashboard → [jeremyacd.github.io/OPs-Dashboard](https://jeremyacd.github.io/OPs-Dashboard/)**

---

## 📁 Repository Files

```
index.html            ← The dashboard application
dashboard-data.json   ← Published data (generated by admin, updated regularly)
README.md             ← This file
```

---

## 🚀 Admin Workflow — Publishing Data

Only the admin needs to upload CSVs. Everyone else sees the data automatically.

### Step 1 — Load your data locally
Download `index.html` from the repo and open it in Chrome. Upload your files using the buttons in the header:

- **📞 CDR Report** — Call Detail Record CSV export (single file)
- **📅 Schedule CSV(s)** — Weekly Data Export files. Hold **Ctrl** (Windows) or **Cmd** (Mac) to select multiple weeks at once for a full month
- **👤 User State Detail** — Actual worked time and state data. Hold **Ctrl/Cmd** to select multiple files if your state export spans multiple date ranges — they are merged automatically

> **Note on large files:** The CDR Report can be processed even if it covers several months of data. A progress indicator will appear showing the file size and processing percentage — just wait for it to complete before uploading the next file.

### Step 2 — Publish
Once all files are loaded, click the **📤 Publish Data** button in the header. This downloads a fresh `dashboard-data.json` to your computer.

### Step 3 — Commit to GitHub
Go to the repo → **Add file → Upload files** → drop in the new `dashboard-data.json` → **Commit changes**. GitHub replaces the old file automatically since the filename is the same.

### Step 4 — Everyone sees it
Anyone visiting the dashboard URL will automatically load your published data — no uploading needed on their end. A green **"Live data · Published by administrator"** banner confirms it loaded successfully.

> **To refresh the data:** repeat Steps 1–3 with new CSVs any time you want to update.

---

## 🔍 Views & Filters

The dashboard supports five view modes, selectable from the top control bar:

| View | What it shows | Date Control |
|---|---|---|
| **Day** | Single date — every metric for that day | Date picker |
| **Week** | 7-day rolling window | ‹ › navigation arrows |
| **Month** | Full calendar month | Date picker (month/year) |
| **Quarter** | Full fiscal quarter (Q1–Q4) | Quarter + Year dropdowns |

When you switch to **Quarter** view the dashboard automatically defaults to the current quarter and year. Use the dropdowns to navigate to any prior quarter for historical reporting.

All metrics, charts, queue tables, staffing, agent performance, and adherence respond to the selected view. The **⬇ Export** button always downloads data matching your current view and any active filters.

---

## 📞 Operations Tab

### Summary Cards
- Service Level %, ASA, AHT, Queue Abandon %, Avg Callback Time
- Goals tracked with color-coded status (green / yellow / red)

### Volume Cards
- Queue Offered, Total Answered, Total Outbound, Total Transferred
- Queue Abandoned, Total Callbacks, Total Denied, Avg Talk Time

### Hourly Charts
- Calls Per Hour, Transfers Per Hour, Denied Calls Per Hour, AHT Per Hour
- Aggregated automatically for Week, Month, and Quarter views

### Special Sections
- **IVR Pre-Queue** — calls that never reached an agent queue
- **Spanish Language Queues** — separate metrics for Spanish queues
- **Chat Support** — separate metrics for chat interactions

### Staffing by Hour
Shows staffed agent count per hour (active agents, break/lunch excluded):
- **Day** — actual count for the selected date
- **Week** — average per hour across the selected week
- **Month** — average per hour across the selected month
- **Quarter** — average per hour across all days in the quarter

### Queue Breakdown Table
Offered, Answered, Transfer, Abandoned, Abandon%, Callbacks, Outbound, Denied, Denied%, Avg Talk, AHT, ASA, SL%

### Agent Performance Table
Fully sortable — click any column header to sort ascending or descending:

| Column | Description |
|---|---|
| Agent | Name (A–Z sortable) |
| Clock In | First NOT READY state of the day |
| Clock Out | Last state end time of the day |
| Adherence | Violation-based % (see policy below) |
| Attend. | Days worked / days scheduled |
| Answered | Total calls answered |
| Transfer | Calls transferred |
| Outbound | Outbound calls made |
| Avg Talk | Average talk time |
| AHT | Average handle time |
| ASA | Average speed of answer |
| Wrap | Average wrap time |
| SL% | Service level percentage |
| Callbacks | Callback calls handled |
| Denied | Denied calls |

Click any agent name to open their detail panel.

---

## ⬇ Export

The **Export** button downloads a CSV file of whatever you are currently viewing, respecting your active view (Day / Week / Month / Quarter) and any filters applied.

### Operations Tab Export
Downloads `agent_performance_[period].csv` — one row per agent with:
- Answered, Transferred, Outbound, Callbacks, Denied
- Avg Talk, AHT, ASA, Avg Wrap, SL%
- Adherence %, Days Worked, Days Scheduled

### Adherence Tab Export
Downloads `adherence_[period].csv` — one row per agent per day with:
- Schedule, Clock In, Clock Out, Lunch
- Adherence %
- Total Violation Minutes
- Individual violation columns: Break Excess, Lunch Excess, ACW Excess, CB Excess, Setup Excess, NR Excess, Late/Early In, Early/Late Out
- Status tag (PTO, FMLA, etc. if marked)

The filename automatically reflects the period — for example `adherence_Q1_2026.csv` or `agent_performance_2026-03.csv`.

> **Tip:** Filter by agent, adherence tier, or clock status before exporting to get a targeted subset.

---

## ⏱ Adherence Tab

### Views
- **Day** — per-agent row with Clock In, Clock Out, Lunch time, Adherence %, and per-policy columns
- **Week** — calendar grid with one cell per day, color-coded adherence and mini violation tags
- **Month** — summary row per agent with violation counts for every policy category
- **Quarter** — same as Month view, aggregated across the full quarter

### Adherence Formula
```
Adherence % = (Scheduled Minutes − Total Violation Minutes) / Scheduled Minutes × 100
```

Every minute over any policy limit is deducted directly from adherence.

### Policy Thresholds

| Policy | Limit | Notes |
|---|---|---|
| Break | 10 min max per instance | Max 2 breaks per shift — 3rd+ break is a full violation |
| Lunch | 30 min max | 1 per shift |
| Additional Call Wrap | 2 min total max | Any overage is a violation |
| Outbound Callback | 3 min total max | Any overage is a violation |
| System Setup | 5 min total max | |
| NOT READY - Please Go Available | 2 min total max | |
| Clock-In (late or early) | ±1 min tolerance | Every minute beyond 1 min reduces adherence |
| Clock-Out (late or early) | ±1 min tolerance | Every minute beyond 1 min reduces adherence |

### Clock-In / Clock-Out Definition
- **Clock-In** = start time of the agent's first **"NOT READY - Please Go Available"** state of the day
- **Clock-Out** = end time of the agent's **last state** of the day

### Filters
- Filter by adherence tier (≥90%, 75–89%, 50–74%, <50%, No Activity)
- Filter by clock status (Late Clock-In, Early Clock-In, Left Early, Stayed Late, On Time)
- Search by agent name

### Absence / Status Tagging
Click any day cell or agent row to mark: PTO, FMLA, Excused Absence, Unexcused Absence, Late Arrival, Left Early, Bereavement, Jury Duty

---

## 📊 Agent Detail Panel

Click any agent name to open a slide-in panel with three tabs:

### 📞 Performance
Call metrics for the selected period: answered, SL%, AHT, ASA, talk time, wrap time, transfers, outbound, callbacks, denied, abandoned, Spanish calls, hourly breakdown chart.

### ⏱ Attendance
Summary stats (avg adherence, days worked, no-shows, late clock-ins, early clock-outs, policy violations) and a daily clock & attendance log with lunch times shown for each day.

### 📊 State Detail
Full state breakdown for the selected period (Day / Week / Month / Quarter):
- Summary cards per state with total minutes and % of shift
- Per-day table: Clock In, Clock Out, Lunch, and every state column with policy limits shown
- Totals row with cumulative minutes and percentages
- States tracked: Productive, Break, Lunch, Add. Call Wrap, Outbound Callback, System Setup, Not Ready, Training, Spanish Callback, Chat Support, Hold, QA / Team Lead

---

## 📊 Queue Abandon % — Calculation Rules

Queue Abandon % only counts **true abandons**:
- Call reached an agent queue (not IVR)
- Customer abandoned with code `Abandoned`
- Customer waited **≥ 60 seconds** before hanging up

Short hangups under 60 seconds and IVR abandonments are excluded from the abandon count and percentage everywhere in the dashboard.

---

## 📂 Input File Formats

### CDR Report
Standard call detail export. Required columns include:
`Direction, Channel Type, Queue, User, Talk Time, Handle Time, Queue Time, Wrap Time, Completion Code, Answered In SLA, Callbacks (Requested), Denied (Previously), Forwarded, Transfer Time, Date / Time`

### Schedule CSV
Weekly export — agent names in column 1, date columns as headers:
```
Agent Name | Sunday 01 Mar 2026 | Monday 02 Mar 2026 | ...
Anders, Devon | | 09:00 AM-05:30 PM | ...
```
Hold **Ctrl/Cmd** when selecting to load multiple weeks at once for a full month or quarter.

### User State Detail
State-by-state activity log. Required columns:
`User Name, State Name, State Attributes, Start Time, End Time, State Duration, Queue, Channel Type`

If your state data is split across multiple export files, select all files at once — the dashboard merges them automatically by keeping the header from the first file and appending data rows from the rest.

---

## 🔗 Sharing Options

| Method | Use case |
|---|---|
| **📤 Publish Data** (recommended) | Commit `dashboard-data.json` to GitHub — all viewers auto-load your data |
| **🔗 Share Link** | Compresses data into the URL — best for small date ranges or quick ad-hoc sharing |

---

## 🛠 Technical Notes

- Runs entirely client-side — no backend, no database, no cookies
- All data stays in the browser; nothing is sent to any external server
- Compatible with Chrome and Edge; best viewed on a widescreen monitor
- Data is compressed with LZ-String before publishing to keep file sizes small
- Published data auto-loads on page open; if data was built with an older version of the dashboard, a red warning banner will prompt the admin to re-publish
- Upload buttons use native HTML `<label>` elements linked to file inputs — this is the most reliable cross-browser approach and works without any JavaScript involvement
